{
  "name": "thunder-rigs-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker relay for Thunder Rigs multiplayer (free SQLite Durable Object).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:kv": "wrangler deploy --config wrangler.kv.toml",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.114.14"
  }
}

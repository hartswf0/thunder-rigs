{
  "name": "thunder-rigs-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker relay for Thunder Rigs multiplayer (free KV tier).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:do": "wrangler deploy --config wrangler.do.toml",
    "kv:create": "wrangler kv namespace create ROOMKV",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.114.14"
  }
}

# INCEPTION — seed for a fresh working context

Paste the prompt below to begin. Everything it references is in this repo.

---

Work on /Users/gaia/Downloads/thunder-rigs (single-file game: index.html,
~18k lines; branch `thunder-film`, merge every verified increment to `main` —
main IS the live GitHub Pages site). Test through the REAL UI with the
`thunder-rigs` preview server (port 8123): cold load → ENTER WORKBENCH →
real clicks. Synthetic shortcuts have repeatedly hidden real bugs (dead
buttons, z-fights, in-ritual traps). Commit each verified increment with a
full story; push branch and main.

Read the five books in film/ first — they govern:
THEORY.md (Law 1: one operational surface — every AI act uses the ritual's
forge weather/build drone/AI·LOG; Law 2: a film is a game loaded through
PLAY) · CINEOSIS.md (the film language: 21-image matrix, camera grammar) ·
LOOK.md (post stack + THE FORGE BAKES) · LEDGER.md (the tracking model +
invariants 1-10) · LIBRARY.md (the holy place: everything generated is kept,
pictured, mood-tagged, placeable).

State: FILM STUDIO works end-to-end with the user's OpenAI key (banner in
studio; use __aiReady(), NEVER aiLocalAvailable — it lies for the dead
'thunder' proxy). Cast = native HELLO rig actors (__castActors, never bots),
fanned + selectable. Sets/stages via __applyForgeFort (ids motif-<mi>-<form>,
shot-<i>). 21-cell prompt cascade (TWIN.imageOf). PERFORM gates, keeps built
geometry, prefers the staged CINE reel. Films shelve to trig.films.v1 → PLAY
list. CSS post + __forgeBake vertex baking live.

THE MISSION — build THE LEDGER and THE LIBRARY as ONE construction
(LEDGER.md build order + LIBRARY.md build order):
window.LEDGER: one row per film entity {id, kind, prompt, code, provenance,
object, placement, physics, cert, shots[], verbs[], state, thumb, tags, mood,
favorite}. Migrate __castActors/__filmBuilt into rows. Placeholders land at
marks the moment a film composes (placement before generation; GEN only ever
swaps the object inside its own row — this fixes generations deleting each
other). Scene graph, PERFORM, and CAMVIZ render/resolve FROM the ledger.
Budget pre-check before forging. Auto-thumbnail entries at bake time
(offscreen render → small dataURL); persist the library index; goldens seeded
as founding entries; LIBRARY panel (search / mood chips / kind tabs /
recently used / favorites; + places at a mark). Then: shot BLOCKING (actors
tween to per-shot marks before the camera cuts, via CINE) and
click-to-POSSESS any cast actor (drive it; click away to release).

Deferred, do not start unless asked: WebGL composer (export-time bloom/DoF),
FORGE THEATER stage events, Cloudflare worker 429 (user's $5 decision).

Begin by reading film/LEDGER.md and film/LIBRARY.md, then build.
